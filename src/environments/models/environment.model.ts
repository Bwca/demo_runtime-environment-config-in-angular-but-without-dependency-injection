export interface Environment {
    api: string;
    and: {
        now: {
            for: {
                something: {
                    completely: {
                        different: string;
                    };
                };
            };
        };
    };
}
